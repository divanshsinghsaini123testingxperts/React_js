1.Router -- React does not have Router(because it is single page application) ,mtlb url bhi to change krna pdega 
2.State Management(Redux toolkit , redux ,Context API)(bez too many variables)
3. Class based - log phle use krte the 
4. BAAS - backend as service(firebase,superbase,appwirte)


you can learn Next.js after React.js(based on react)
Next.js - we can write frontend and backend in same framework 
